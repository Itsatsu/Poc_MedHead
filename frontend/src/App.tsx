import { BedAllocationForm } from './components/BedAllocationForm';
import './App.css';

// Point d'entrée de l'UI : la PoC ne comporte qu'un seul écran (le formulaire d'allocation).
function App() {
  return <BedAllocationForm />;
}

export default App;
