spring:
  application:
    name: allocation-lits

medhead:
  cors:
    allowed-origins: ${MEDHEAD_CORS_ALLOWED_ORIGINS:http://localhost:5173}
  distance:
    google-maps:
      api-key: ${GOOGLE_MAPS_API_KEY:}
    circuit-breaker:
      failure-threshold: 3
      open-duration-seconds: 30
